# SOP adapter (transplanted skills)

All copied stage skills must:

1. Set `disable-model-invocation: true`
2. State artifact root from `sop-orchestrator/workspace-map.md`
3. Refuse to treat this SOP repo's `projects/<slug>/` as their product tree (except TEST/DOCS)

When harvesting method updates inside a transplanted skill, propose the patch and wait for user confirm; optionally mirror back to the source workspace.
